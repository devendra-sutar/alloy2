 docker push  devendra750/monitoringstack_image:v15 
The push refers to repository [docker.io/devendra750/monitoringstack_image]
0de8231456d1: Pushed 
611d87277e1f: Pushed 
a66257b9e5c6: Layer already exists 
5c9cbac555d4: Layer already exists 
dbd3b434f41b: Layer already exists 
470b66ea5123: Layer already exists 
v15: digest: sha256:ec11e4cdc1ebcba1fb7cffe203ac6df91c8323326fb751a255428cb3979fd8cb size: 1581
root@devendra-ws:/home/ubuntu/git-monitoring-img# 
root@devendra-ws:/home/ubuntu/git-monitoring-img# kubectl apply -f /home/ubuntu/tools.yaml 
serviceaccount/onboarding-sa created
clusterrolebinding.rbac.authorization.k8s.io/onboarding-sa-cluster-admin created
configmap/onboarding-config created
job.batch/k8s-onboarding-job created
root@devendra-ws:/home/ubuntu/git-monitoring-img# kubectl get pod
NAME                       READY   STATUS              RESTARTS   AGE
k8s-onboarding-job-q6l7v   0/1     ContainerCreating   0          4s
root@devendra-ws:/home/ubuntu/git-monitoring-img# kubectl get pod --watch
NAME                       READY   STATUS              RESTARTS   AGE
k8s-onboarding-job-q6l7v   0/1     ContainerCreating   0          11s
k8s-onboarding-job-q6l7v   0/1     Error               0          11s
k8s-onboarding-job-q6l7v   0/1     Error               0          13s
k8s-onboarding-job-q6l7v   0/1     Error               0          13s
k8s-onboarding-job-q6l7v   0/1     Error               0          14s
^Croot@devendra-ws:/home/ubuntu/git-monitoring-img# kubectl logs k8s-onboarding-job-q6l7v 
===== VARIABLES CONFIRMED =====
CLUSTER_NAME    = Core-infra
USERNAME        = admin
OS_USERNAME     = default-user
OS_PASSWORD     = default-password
PROMETHEUS_HOST = http://172.16.1.107:30090
LOKI_HOST       = http://10.0.32.103:3100
API_HOST        = 172.16.1.188:8006
API_URL         = https://172.16.1.188:8006/api/v2.0/status/update/
NAMESPACE       = onboard
OUTPUT_DIR      = /tmp
VALUES_TEMPLATE = values-template.yaml
================================
[INFO] Sending status to API: {"cluster_name":"Core-infra","status":"Tool Installation Started"}
{"message":"status updated","cluster_name":"Core-infra","status":"Tool Installation Started","storage":"redis","processing_time_ms":1.31}serviceaccount/admin created
clusterrolebinding.rbac.authorization.k8s.io/admin-cluster-admin unchanged
/usr/local/bin/deploy.sh: line 112: log_success: command not found
root@devendra-ws:/home/ubuntu/git-monitoring-img# cat Dockerfile 
FROM ubuntu:20.04

# Set non-interactive frontend for apt
ENV DEBIAN_FRONTEND=noninteractive

# Install dependencies
RUN apt-get update && apt-get install -y \
    curl \
    git \
    sudo \
    gettext-base \
    && rm -rf /var/lib/apt/lists/*

# Install kubectl
RUN curl -LO "https://dl.k8s.io/release/v1.28.0/bin/linux/amd64/kubectl" \
    && chmod +x kubectl \
    && mv kubectl /usr/local/bin/

# Install Helm
RUN curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

# Copy the deployment script
COPY deploy.sh /usr/local/bin/deploy.sh
RUN chmod +x /usr/local/bin/deploy.sh

# Set entrypoint
ENTRYPOINT ["/usr/local/bin/deploy.sh"]
root@devendra-ws:/home/ubuntu/git-monitoring-img# cat deploy.sh 
#!/usr/bin/env bash
set -euo pipefail

### ===================
### Positional args
### ===================
CLUSTER_NAME="${1:-}"; shift || true
USERNAME="${1:-${K8S_USERNAME:-admin-user}}"; shift || true
PROMETHEUS_HOST="${1:-}"; shift || true
LOKI_HOST="${1:-}"; shift || true
API_HOST="${1:-}"; shift || true

### ===================
### Default Values
### ===================
NAMESPACE="onboard"

DEFAULT_OS_USERNAME="default-user"
DEFAULT_OS_PASSWORD="default-password"

OS_USERNAME="${OS_USERNAME:-$DEFAULT_OS_USERNAME}"
OS_PASSWORD="${OS_PASSWORD:-$DEFAULT_OS_PASSWORD}"
OUTPUT_DIR="/tmp"
VALUES_TEMPLATE="values-template.yaml"


### ===================
### API_HOST from environment or fallback
### ===================
API_HOST="${API_HOST:-}"
API_PATH="/api/v2.0/status/update/"
API_URL=""
if [[ -n "$API_HOST" ]]; then
  if [[ "$API_HOST" =~ ^https?:// ]]; then
    API_URL="${API_HOST%/}${API_PATH}"
  else
    API_URL="https://${API_HOST%/}${API_PATH}"
  fi
fi

### ===================
### Export
### ===================
export CLUSTER_NAME USERNAME PROMETHEUS_HOST LOKI_HOST OS_HOST
export API_HOST API_URL
export NAMESPACE OS_USERNAME OS_PASSWORD

### ===================
### Confirmation Output
### ===================
echo "===== VARIABLES CONFIRMED ====="
echo "CLUSTER_NAME    = $CLUSTER_NAME"
echo "USERNAME        = $USERNAME"
echo "OS_USERNAME     = $OS_USERNAME"
echo "OS_PASSWORD     = $OS_PASSWORD"
echo "PROMETHEUS_HOST = $PROMETHEUS_HOST"
echo "LOKI_HOST       = $LOKI_HOST"
echo "API_HOST        = $API_HOST"
echo "API_URL         = $API_URL"
echo "NAMESPACE       = $NAMESPACE"
echo "OUTPUT_DIR      = $OUTPUT_DIR"
echo "VALUES_TEMPLATE = $VALUES_TEMPLATE"

echo "================================"


STATUS_MSG="Tool Installation Started"
PAYLOAD="{\"cluster_name\":\"${CLUSTER_NAME}\",\"status\":\"${STATUS_MSG}\"}"

if [[ -z "${API_URL:-}" ]]; then
  echo "[WARN] API_URL not set — skipping status update: ${STATUS_MSG}"
else
  echo "[INFO] Sending status to API: ${PAYLOAD}"
  curl -k --silent --show-error --fail \
       -H "Content-Type: application/json" \
       -d "${PAYLOAD}" \
       "${API_URL}" \
    || echo "[WARN] API call failed for status: ${STATUS_MSG}"
fi

### Sanitize SA name
SA_NAME=$( echo "$USERNAME" | tr '[:upper:]' '[:lower:]' | sed 's/[^a-z0-9.-]/-/g' | sed 's/^-*//;s/-*$//' )
if ! [[ "$SA_NAME" =~ ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$ ]]; then
  log_error "Invalid SA name: '$SA_NAME'"
  send_status "Installation Failed"
  exit 1
fi


### Step 2: ServiceAccount + ClusterRoleBinding
kubectl get ns "$NAMESPACE" >/dev/null 2>&1 || kubectl create namespace "$NAMESPACE" >/dev/null 2>&1
kubectl apply -f - <<EOF
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${SA_NAME}
  namespace: ${NAMESPACE}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ${SA_NAME}-cluster-admin
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: ${SA_NAME}
  namespace: ${NAMESPACE}
EOF
log_success "ServiceAccount + ClusterRoleBinding created"
STATUS_MSG="Tool Installtion Started"


# Create values template
echo "Creating values template..."
cat > "${OUTPUT_DIR}/${VALUES_TEMPLATE}" <<'EOF'
config:
  logLevel: error
  logFormat: json
  route:
    routes:
      - match:
        - receiver: "dump"
        drop:
        - namespace: "event-exporter"
        - type: "Normal"
  receivers:
    - name: "dump"
      opensearch:
        hosts:
          - "${OS_HOST}"
        index: "kube-events"
        indexFormat: "kube-events"
        username: "${OS_USERNAME}"
        password: "${OS_PASSWORD}"
        useEventID: true
        deDot: true
        tls:
          insecureSkipVerify: true
        layout:
          eventType: "kubernetes-event"
          createdAt: "{{ .GetTimestampMs }}"
          details:
            message: "{{ .Message }}"
            reason: "{{ .Reason }}"
            type: "{{ .Type }}"
            count: "{{ .Count }}"
            kind: "{{ .InvolvedObject.Kind }}"
            name: "{{ .InvolvedObject.Name }}"
            namespace: "{{ .Namespace }}"
            component: "{{ .Source.Component }}"
            host: "{{ .Source.Host }}"
            labels: "{{ toJson .InvolvedObject.Labels}}"
EOF

echo "Template written to ${OUTPUT_DIR}/${VALUES_TEMPLATE}"

# Render values.yml
echo "Rendering values.yml..."
export OS_USERNAME OS_PASSWORD OS_HOST CLUSTER_NAME SA_NAME
envsubst < "${OUTPUT_DIR}/${VALUES_TEMPLATE}" > "${OUTPUT_DIR}/values.yml"
if [[ ! -s "${OUTPUT_DIR}/values.yml" ]]; then
  echo "[ERROR] Rendered values.yml is empty!" >&2
  exit 1
fi
echo "Created ${OUTPUT_DIR}/values.yml"

# Render event-exporter.yaml
echo "Rendering event-exporter.yaml..."
envsubst < "${OUTPUT_DIR}/${VALUES_TEMPLATE}" > "${OUTPUT_DIR}/event-exporter.yaml"
if [[ ! -s "${OUTPUT_DIR}/event-exporter.yaml" ]]; then
  echo "[ERROR] Rendered event-exporter.yaml is empty!" >&2
  exit 1
fi
echo "Created ${OUTPUT_DIR}/event-exporter.yaml"

echo "Done rendering templates."

if ! kubectl get namespace "$NAMESPACE" >/dev/null 2>&1; then kubectl create namespace "$NAMESPACE"; fi

helm repo add bitnami https://charts.bitnami.com/bitnami
helm repo update
helm install event-exporter bitnami/kubernetes-event-exporter -n "$NAMESPACE" -f "/tmp/values.yml"  --set image.repository=bitnamilegacy/kubernetes-event-exporter

STATUS_MSG="Installation In Progress..."


helm repo add grafana https://grafana.github.io/helm-charts
helm repo update 
helm upgrade --install my-release grafana/k8s-monitoring --version 1.5.1 -n "$NAMESPACE" --create-namespace --skip-crds --set opencost.enabled=false --set cluster.name="$SA_NAME" --set externalServices.prometheus.host="$PROMETHEUS_HOST" --set externalServices.prometheus.writeEndpoint=/api/v1/write --set externalServices.loki.host="$LOKI_HOST"



helm repo add kepler https://sustainable-computing-io.github.io/kepler-helm-chart
helm repo add opencost https://opencost.github.io/opencost-helm-chart
helm upgrade --install kepler kepler/kepler -n "$NAMESPACE"
helm upgrade --install opencost opencost/opencost -n "$NAMESPACE"
kubectl patch deployment opencost -n "$NAMESPACE" --type='json'  -p='[{"op":"replace","path":"/spec/template/spec/containers/0/env","value":[{"name":"PROMETHEUS_SERVER_ENDPOINT","value":"'"$PROMETHEUS_HOST"'"},{"name":"INSECURE_SKIP_VERIFY","value":"true"}]}]'
kubectl -n onboard set env deployment/opencost CUSTOM_PRICING_ENABLED=true 
kubectl -n onboard set env deployment/opencost CLOUD_COST_ENABLED=true


### ================================
### Create config.alloy for Alloy
### ================================
cat > "${OUTPUT_DIR}/config.alloy" <<EOF
prometheus.scrape "opencost" {
  targets = [{"__address__" = "opencost.${NAMESPACE}.svc.cluster.local:9003"}]
  scrape_interval = "1m"
  metrics_path = "/metrics"
  forward_to = [prometheus.remote_write.central.receiver]
}
prometheus.scrape "kepler" {
  targets = [{"__address__" = "kepler.${NAMESPACE}.svc.cluster.local:9102"}]
  scrape_interval = "1m"
  metrics_path = "/metrics"
  forward_to = [prometheus.remote_write.central.receiver]
}
prometheus.scrape "trivy_operator" {
  targets = [{"__address__" = "trivy-operator.${NAMESPACE}.svc.cluster.local:80"}]
  scrape_interval = "1m"
  metrics_path = "/metrics"
  forward_to = [prometheus.remote_write.central.receiver]
}
prometheus.remote_write "central" {
  endpoint {
    url = "${PROMETHEUS_HOST}/api/v1/write"
    tls_config {
      insecure_skip_verify = true
    }
  }
  external_labels = { cluster = "${CLUSTER_NAME}", username = "${SA_NAME}" }
}
EOF

echo "Created ${OUTPUT_DIR}/config.alloy"
kubectl create configmap alloy-config --namespace "$NAMESPACE" --from-file=config.alloy="${OUTPUT_DIR}/config.alloy" --dry-run=client -o yaml | kubectl apply -f -
cat > "${OUTPUT_DIR}/values-alloy.yaml" <<EOF
alloy:
  configMap:
    create: false
    name: alloy-config
    key: config.alloy
EOF

echo "Created ${OUTPUT_DIR}/values-alloy.yaml"
helm upgrade --install alloy grafana/alloy -n "$NAMESPACE" -f "${OUTPUT_DIR}/values-alloy.yaml"



helm repo add aquasecurity https://aquasecurity.github.io/helm-charts
helm repo update
cat > "${OUTPUT_DIR}/trivy-values.yaml" <<EOF
operator:
  metricsVulnIdEnabled: true
serviceMonitor:
  enabled: true
trivy:
  ignoreUnfixed: true
service:
  headless: false
EOF

echo "Created ${OUTPUT_DIR}/trivy-values.yaml"
helm upgrade --install trivy-operator aquasecurity/trivy-operator -n "$NAMESPACE" -f "${OUTPUT_DIR}/trivy-values.yaml"



 kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
 kubectl patch deployment metrics-server -n kube-system --type='json' -p='[{"op":"add","path":"/spec/template/spec/containers/0/args/-","value":"--kubelet-insecure-tls"}]'

STATUS_MSG="Cluster Onboarded Succesfully"
root@devendra-ws:/home/ubuntu/git-monitoring-img# 


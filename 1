if ! retry_helm helm upgrade --install event-exporter  oci://registry-1.docker.io/bitnamicharts/kubernetes-event-exporter -n "$NAMESPACE" -f "/tmp/values.yml" --set im>
  STATUS_MSG="Installation Failed"
  echo "[ERROR] event-exporter install failed after retries"
  exit 1
  # send_status here if you want
fi

